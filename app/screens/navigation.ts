export type RootStackParamList = {
  MarketHome: {
    placeholderListing: {
      id: string;
      title: string;
      price: number;
      isPlaceholder: boolean;
    };
  };
  // Add other screens here as needed
};
